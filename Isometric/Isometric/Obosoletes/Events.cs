namespace Isometric.Obsoletes
{
    //The events that can occur for subscribers to handle
    public enum Events
    {
        //Input Events

        //Events for keys pressed once
        KEY_PRESSED_O,

        //Events for keys pressed and held down
        KEY_PRESSED_ENTER,
    }
}
